# Phase 2 — LangGraph agent pipeline (not yet implemented)

See `docs/ARCHITECTURE.md` for the planned graph:
`triage_failure → retrieve_context → generate_patch → sandbox_test → open_pr | file_issue`
