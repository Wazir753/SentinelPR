# SentinelPR — Python dependencies
# Phase 1: webhook ingress + ChromaDB indexing

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
httpx>=0.27.0
python-dotenv>=1.0.0

# Retrieval (Phase 1)
chromadb>=0.5.0
sentence-transformers>=3.0.0
GitPython>=3.1.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0

# Phase 2+ (declared now; wired in later phases)
# langgraph
# langchain-core
# PyGithub
